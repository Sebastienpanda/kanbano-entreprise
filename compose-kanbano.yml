version: "3.9"

services:
    kanbano:
        build:
            context: .
            dockerfile: ./apps/kanbano/Dockerfile
        container_name: kanbano
        restart: always
        ports:
            - "80:80"
            - "443:443"
